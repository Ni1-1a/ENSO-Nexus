Сопоставь объекты участка из описи с ограничениями, названными в документах,
и выдай правило на каждую пару. Верни строго JSON по схеме.
Помни: правило, а не координаты. Чего не хватает — в missingData.
